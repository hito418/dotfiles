# Git & Commits

- **Conventional commits** with semantic versioning (e.g., `feat:`, `fix:`, `refactor:`, `docs:`).
- Commit messages explain *why*, not just *what*.
- **No Co-Authored-By line** in commit messages.
- Rebase on pull (`pull.rebase = true`).
- GPG-sign commits.
