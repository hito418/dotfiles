# Git & Commits

- **Conventional commits**: `feat:`, `fix:`, `refactor:`, `style:`, `test:`, `docs:`, `chore:`, `build:`, `ci:`.
- Subject line ≤72 chars. Explain *why*, not *what*.
- Add a body only when the *why* isn't obvious from the subject.
- No `Co-Authored-By` lines.
